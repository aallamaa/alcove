[TODO] setup etag to index with emacs
[DONE] (loaddb) command + auto-load at startup (skip with --noload / -n)
[DONE] persistence: phase 1 — EXP_NUMBER, EXP_FLOAT, EXP_SYMBOL added
       to exp_tfuncList alongside the pre-existing EXP_CHAR/STRING
[DONE] persistence: phase 2 — EXP_PAIR (recursive, 1-byte presence flag
       per child for nil + improper pairs) + EXP_LAMBDA via source-form
       (name + params + body trees, re-compile on load); lambdas survive
       across runs and re-install JIT on the new arch
[DONE] savedb warns on stderr when an entry's type isn't __DUMPABLE__
       (was silently dropping; now surfaces at save time)
[DONE] BUG: (do (id-fn arg) other) returned nil for an identity id-fn.
       No longer reproduces (verified (do (f 1) 42) => 42 on parser/jit).
       Resolved once closures/do-bodies compile cleanly; regression test
       "do: value after identity call" in test.alc.

[DONE] add amd64 JIT backend alongside existing arm64 (auto-detected via
       uname -m; fib went 1.5x slower → 2.3x faster than python on x86_64)
[DONE] make `make` default to JIT release; add `make nojit` opt-out
[DONE] generalize JIT leaf shapes to any slot < ENV_INLINE_SLOTS
       (was hardcoded slot 0 → only single-param functions could JIT)
[DONE] add multiplication-by-constant leaf JIT shape (* n K) on both
       backends; tag arithmetic via (t-1)*K + 1
[DONE] add (do ...) to the bytecode compiler (was always failing → AST
       fallback, which blocked any downstream JIT on bodies using do)
[DONE] amd64 JIT for tail loop with one inner global call (the fact
       wrapper shape); needs JIT-to-runtime callout via C trampoline
[DONE] amd64 JIT for two-call non-tail recursion (the fib shape); two
       value-returning callouts + saved first-result on stack + tagged add
[DONE] add (disasm fn) built-in for inspecting compiled lambda bytecode
       + JIT install status (replaces ad-hoc fprintf debugging in C)
[DONE] verify arm64 backend end-to-end via qemu-aarch64 cross-compile
       (72/72 tests pass on both arches; same outcomes)
[DONE] fix arm64 sequence-point UB in try_jit_simple_tail_loop branch
       displacement (out[n++] = arm64_b(loop_top - n) was unspecified-order)
[DONE] fix macro / code expand-macro1 (fixed commit 90a7e69da94ebdd0e2781a22dd559484d216736b )
[TODO] should we store 8 bytes values directly inside content pointer of pair instead of pointing to another object
[TODO] fix error handling / freeing ressources with setjmp (gave up using setjmp too many side effects 1d339c702754297eb5b8c324f2713670da125dd2)
[DONE] utf-8 in string indexing and char: length/indexing/substr/
       string-index/split count by codepoint (not byte); chars hold a
       full codepoint and read/print/persist (savedb) as UTF-8; index
       assignment (= (s i) ch) rebuilds for variable width; als
       transpiler reads multi-byte #\X literals. utf8_* helpers in
       alcove.c. Tests: "utf8 *" block in test.alc.
[TODO] add support for documentation in function header
[TODO] add support for default parameters
[TODO] add support for polymorphism in lambda
[TODO] add support for continuation call/cc
[TODO] add support for http://wiki.msgpack.org/display/MSGPACK/Format+specification
[TODO] check added value of http://www.canonware.com/jemalloc/ instead of malloc
[TODO] see https://www.hackerschool.com/blog/13-list-comprehensions-in-eight-lines-of-clojure
[TODO] see concatenative stuff: http://blog.fogus.me/2013/01/06/pesto5-a-concatenative-programming-library-in-5-lines-of-clojure/
[TODO] check qsort http://arclanguage.org/item?id=16705
[TODO] implement http://en.wikipedia.org/wiki/Hash_array_mapped_trie
[TODO] check https://github.com/clojure/clojure/blob/master/src/jvm/clojure/lang/PersistentHashMap.java
[TODO] check https://github.com/chaelim/HAMT lock free: https://github.com/axel22/Ctries 
[TODO] implement http://gcc.gnu.org/onlinedocs/gcc-4.4.5/gcc/Atomic-Builtins.html for every potentially concurrent objects
[TODO] implement JIT with DynASM http://blog.reverberate.org/2012_12_01_archive.html
[TODO] check http://nanocubes.net/
[TODO] check http://en.wikipedia.org/wiki/Splay_tree
[TODO] check http://en.wikipedia.org/wiki/Suffix_trie
[TODO] check http://en.wikipedia.org/wiki/Rope_%28data_structure%29 
[TODO] check http://cr.yp.to/critbit.html 
[TODO] check http://en.wikipedia.org/wiki/Dancing_Links 
[TODO] check http://en.wikipedia.org/wiki/Skip_list
[TODO] check http://okasaki.blogspot.co.uk/2008/02/ten-years-of-purely-functional-data.html
[TODO] check http://en.wikipedia.org/wiki/Binary_space_partitioning 
[TODO] check http://adtinfo.org/index.html

[DONE] REPL: readline tab completion + history + multi-line continuation
       (auto-detected via /usr/include/readline/readline.h presence)
[DONE] closures: (fn ...) and (def ...) capture defining env; mutating
       closures via walk-up updatebang ((= captured-var ...) finds the
       outer slot); 0-arg defs fixed (var2env empty-params bug)
[DONE] zero-cost stdlib batch: mod abs max min length nth reverse
       append number? string? symbol? pair? fn? exit quit random apply
       map filter reduce — 18 builtins, no perf impact (hot paths
       untouched)
[DONE] OP_SLOT_LE_SLOT superinst — fuses LOAD_SLOT+LOAD_SLOT+LE for
       compile_for; forsum 21→18ms (parity with python)
[DONE] benchmarks: ackermann, listsum, sieve added; ackermann 2.2x
       faster than python, listsum 1.8x faster, sieve 7x slower (deep
       AST recursion + interp int — known weakness)
[DONE] FFI: (ffi-fn LIB FN-NAME RET-TYPE ARG-TYPES...) via libffi;
       supports void/int/long/double/string/ptr; max 8 args; full
       examples + README in ffi-examples/
[DONE] documented test 95/1 on amd64 + arm64 (qemu)

[DONE] BUG: (filter pred (cdr xs)) inside a recursive fn evaluated to
       blank. ROOT CAUSE (not refcount): (cdr <1-element list>) returns
       C NULL (lists terminate with NULL, not NIL_EXP), and filter/map/
       reduce/any?/all?/sort-by guarded `if (!fn || !xs)` — rejecting the
       NULL list as a missing parameter and erroring. Fix: require the
       list-arg FORM (arity) but treat a NULL VALUE as the empty list.
       Regression tests "filter inside recursion" + per-builtin
       "(cdr 1-elem)" cases in test.alc.

[TODO] Reserved-name shadowing: lookup() checks reserved_symbol BEFORE
       env, so `(= time (ffi-fn ...))` doesn't take effect at top level
       (alcove's builtin time wins). Either flip lookup order or
       provide an explicit "shadow" form.

[TODO] FFI: support struct-by-value, varargs (printf), callbacks (qsort
       comparator). Each needs a libffi closure setup but no fundamental
       blocker.

[DONE] MEMORY LEAK: closure↔captured-env refcount cycle. A def/let closure
       created INSIDE a function body leaked, scaling with closures created
       (top-level defs immune). Root cause was TWO compounding bugs: (1) a
       per-call refcount leak — evaluate's (operator args) dispatch leaked
       the owned tmpexp2 ref after the non-tail invoke (every sibling branch
       released it; the invoke branch forgot), pinning env-resolved closures;
       (2) the genuine lambda↔env 2-cycle. Fix: add the missing
       unrefexp(tmpexp2) + env_break_self_cycle() in destroy_env (Option B,
       conservative guards). Repro 120MB→2MB flat; 951/0 + ASan-clean.
       Full writeup in docs/closure_env_cycle_leak.md; tests "20c" in
       test.alc.

[DONE] security/memory audit (2026-05): fixed FFI ->ptr crash (ffi-fn arg
       type read raw union as a pointer → SIGSEGV; now exp_text), RESP
       write-buffer DoS ceiling (RESP_WBUF_MAX + wfail drop), unchecked
       reallocs → graceful_shutdown, KEYS* partial-OOM double-free,
       make_inline_txt negative-length guard, non-Apple JIT W^X (mprotect
       W→RX), and vec int32 capacity overflow/UB. deserializer + core
       refcount model audited clean.

[DONE] Compile lambda for closures (was AST-only). Closures now compile
       to bytecode with bc->no_gcache set, so OP_LOAD_GLOBAL/CALL_GLOBAL
       do a fresh env-chain lookup each time instead of a per-bc gcache
       hit (which would serve stale values for a captured var). Closures
       are not JIT'd (the JIT global-call helpers cache).

[DONE] Compile mutating-free-var closures (OP_STORE_FREE). `=`/`setf` to
       a non-slot target (a captured free var or a global) used to fail
       compilation and fall back to AST. New OP_STORE_FREE op +
       assign_store_symbol() walk the env chain at runtime, mutating the
       nearest existing binding (else creating in the CURRENT env — the
       only difference from setq_store_symbol, which creates in root),
       so make-counter and friends now compile fully. Also fixed: the
       compile_lambda param loop refused 0-arg bodies (empty params `()`
       = nil_singleton with NULL content, misread as a malformed param);
       now matches var2env's `p->content` guard, so 0-arg fns compile.
       Reserved-name assignment is rejected at runtime by OP_STORE_FREE,
       same as OP_SETQ_DYN.

[DONE] OP_MOD bytecode op + JIT shape for (fn (a b) (is (mod a b) K))
       — divides? was running as AST because mod was an unrecognized
       builtin. Now: mod is one bytecode op; divides? compiles to 10
       bytes + JITs to ~70 bytes of native (idiv + cmovz). sieve raw
       17ms → 6ms.
[DONE] (any? pred list) and (all? pred list) builtins — walk in C
       with one vm_invoke_values per element. Useful when called
       outside recursive contexts; see TODO below for the bug that
       limits use inside.

[DONE] BUG: (do (fn-call) other) lost `other`, breaking recursion.
       No longer reproduces — both the (g 5) and (any? ...) repros now
       recurse fully (verified => 4). Resolved by the closure/do-body
       compilation work (these fns now compile to bytecode instead of
       hitting the AST flow bug). Regression tests "do: call then tail
       recurse" and "do: any? then tail recurse" in test.alc.

[WATCH] possible (def f () ...) body-form drop when body is a (case ...)
       over multi-form (do ...) arms, in a tight (while ... (do ... (f)
       ...)) loop. Never minimized; suspected related to the identity-
       lambda OP_POP-after-do bug, which is now fixed — so this is likely
       resolved too. Left as a watch item; reopen with a concrete repro
       if it resurfaces.
