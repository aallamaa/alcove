; Ackermann(3, 7) = 1021 — small tree recursion exercising call overhead.
(def ack (m n)
  (if (is m 0) (+ n 1)
      (if (is n 0) (ack (- m 1) 1)
          (ack (- m 1) (ack m (- n 1))))))
(prn (ack 3 9))
